name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (do not actually publish)'
        required: false
        default: 'false'

permissions:
  contents: read
  id-token: write   # required for OIDC token exchange with npm

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish   # protects publish with an optional approval gate
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@6c439dc8bdd0c9e700db98a04ae5cdcfe54855f0 # v2
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Publish to npm
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            npm publish --dry-run --access public
          else
            npm publish --provenance --access public
          fi
        # No NODE_AUTH_TOKEN needed - npm exchanges the GitHub OIDC token
        # automatically when a Trusted Publisher is configured on npmjs.com
