name: "Approve Dependabot PR's" 
on:
  pull_request:

permissions:
  contents: write
  pull-requests: write
  actions: write
  
jobs:
  dependabot:
    uses: devops-actions/.github/.github/workflows/approve-dependabot-pr.yml@1025096c3a41498850f3457dfec7bd1a643424ef # main
    secrets:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
